package org.conectrix.fs.utils;

/**
 * The type File system util.
 */
public class FileSystemUtil {

    /**
     * Is empty boolean.
     *
     * @param str the str
     * @return the boolean
     */
    public static boolean isEmpty(String str) {
        return (str == null || str.isEmpty());
    }

}
